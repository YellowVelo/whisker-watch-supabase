# Launch Punch List

Tracked list of everything outstanding before Wysker Watch goes to the App Store / Play Store, ranked by priority. Pulled from code audits and session history as of 2026-07-10.

Check items off as they're resolved. Add new items at the bottom of their tier, don't renumber existing ones.

---

## P0 — Data safety & process (fix regardless of launch timing)

These aren't App Store requirements, but shipping to real users without them is reckless — one already caused a real incident.

- [ ] **No database backups / PITR configured.** Confirmed on the live project: `pitr_enabled: false`, `backups: []`. Already bit us once — an entire `auth.users` account got deleted and was only recoverable because a stray migration file happened to contain the original data as INSERTs. That safety net won't exist for anything created since. Needs a paid Supabase tier with PITR or a scheduled `pg_dump` export, before real user data accumulates.
- [ ] **No branch protection / CI gate on `main`.** No required reviews, no required checks — every push goes straight to production. Decide whether that's still acceptable once this is a shipped app with real users on it.
- [ ] **No dev/staging environment.** Local `npm run dev` and production point at the same Supabase project — any local testing writes real rows to live data.
- [ ] No automated test coverage on the destructive Edge Functions (`delete-pet`, `delete-account`). The vitest suite (59 tests) covers frontend logic only, not these Deno functions. Accepted risk when Phase 1/2 shipped — worth revisiting now that these are irreversible, no-backup operations (see above).

## P1 — App Store hard blockers

Nothing below this line can go to either store without the native shell existing.

- [ ] **Capacitor wrapping for iOS/Android.** Not started — no `ios`/`android` folders, no `capacitor.config`. This is the root blocker; everything else in this tier and P2's deep-linking item cascade from this decision.
- [ ] **Universal Links / App Links (mobile deep linking).** `https://www.wyskerwatch.com/accept-invite?...` links need to open the app directly, not a browser, once it's a real app — reviewers will test the invite flow. Needs: `apple-app-site-association` + `assetlinks.json` hosted at `www.wyskerwatch.com/.well-known/`, iOS Associated Domains capability, Android `intent-filter` with `autoVerify="true"`, and Capacitor's `@capacitor/app` `appUrlOpen` listener wired to route the incoming URL. Requires an Apple Developer account.
- [ ] **Store submission assets don't exist.** App icons (all required sizes), screenshots, store listing copy, privacy manifest, permissions justification — none of this exists since there's no native project to hold them yet.
- [x] **No in-app Terms of Service**, and no in-app link to the Privacy Policy. `privacy-policy.md` exists in the repo but both stores require these reachable *from within the app*, not just a repo file. Resolved: Menu → Terms of Service (`/terms`) and Menu → Privacy (`/privacy`) are both live, each with a list screen and per-section detail screens (`src/pages/Terms.jsx`, `src/pages/TermsOfServiceSection.jsx`, `src/lib/termsOfServiceContent.js`; `src/pages/Privacy.jsx` et al.).
- [x] **`manifest.json` is missing** — stale, resolved. This was true before `vite-plugin-pwa` was added; `public/` now exists with real icon assets, and the plugin generates and injects `manifest.json` at build time (`vite.config.js`'s `VitePWA({...})` config). `index.html`'s source has no manifest `<link>` because it's injected into the built output, not hand-authored. Full PWA behavior (manifest, service worker, install prompts, offline shell) is now documented in `docs/features/PWA Feature.md`.

## P2 — Security / trust issues

Could cause real harm (spam, data leakage, silent failures) to real users post-launch.

- [ ] **Test/demo account email suppression isn't centralized.** `invite-co-owner`'s old path skipped real sends for `test`/`demo` accounts; the new shared `sendEmail()` doesn't. Needs an optional `sentByUserId` param that looks up `account_type` and skips the real Resend call (still logs the attempt) for test/demo triggers. Build into the first real workflow that calls `sendEmail`, not speculatively.
- [ ] **No bounce/delivery-webhook handling.** `email_logs.status = 'sent'` only means "Resend accepted it," not "it arrived." Needs a `resend-webhook` Edge Function verifying Resend's signature and updating `email_logs` by `provider_message_id` on delivery/bounce/complaint events.
- [ ] **Demo Account Phase 3 has no read-only enforcement.** `demo1@wyskerwatch.com` shows the DEMO MODE banner but can add/edit/delete data exactly like a real account — only seeding + admin-gating of the reset/seed tools got built.
- [ ] Sitter invite emails never actually send — access records are created but no email fires. Confirmed no sitter-invite email code exists in `supabase/functions/`.

## P3 — Product/data decisions needing your sign-off

Not code defects — need an explicit decision because they affect what the app tells users about their pets' health.

- [x] **Direction/severity values for bathroom, stool, mobility, breathing, itching — resolved as a product decision, not a vet-review blocker.** Per-option severity grading (`observation_options.severity_score`/`health_score_deduction`/`direction_ordinal` from migrations 0014/0021) has been replaced for the 5 Health Attributes + 4 Wellbeing attributes with an explicit, equal-weight model (migration `0022_health_score_equal_weight_multiselect.sql`, `src/lib/checkin/scoring.js`): these categories are now multi-select, every symptom logged carries equal weight (owners are reporting observations, not a clinical assessment — no vet signoff applies), and each attribute's deduction is `min(count of symptoms, 2)`. `severity_score`/`health_score_deduction`/`direction_ordinal` columns are left in place but unused by the new logic (legacy V1 rings still read `severity_score`, untouched/out of scope).
- [x] **Baseline defaults to a global "normal," not a per-pet baseline — the ambiguity part is resolved.** Every completed check-in now writes an explicit row per multi-select category every day (a confirmed-"normal" baseline row when nothing was selected, one row per symptom otherwise — `markNormal`/`saveChangedCheckIn` in `src/lib/checkin/checkinClient.js`), so "no row" is never inferred/assumed. `pet_baselines` staying unpopulated and severity/weight staying flat-and-global (not per-pet) remains the deliberate design decision from a prior session — not reopened here.
- [ ] **Trends Feature Spec's "compare against the pet's baseline" business rule is dropped, not deferred — revisit if per-pet baselines ever become real.** `0010 Trends Feature Specification_v2.txt` (Business Rules) says "Trend calculations always compare against the pet's baseline where available." The actual build has never done this — severity/deduction/direction values are flat and global for every pet, `pet_baselines` is populated as an informational reference only and is never read as a comparison multiplier anywhere in `scoring.js`/`checkinClient.js`/`trendsClient.js` (decision reconfirmed this session when the Health Score moved to equal-weight symptom counts). If per-pet baselines are ever wired into scoring/trend logic as a real feature, this line becomes relevant again — until then, the new version of the Trends spec should drop it rather than describe unbuilt behavior.
- [x] **Historical Health Score backfill — done.** Migration `0022_health_score_equal_weight_multiselect.sql` backfills explicit baseline observation rows for every historical completed check-in and recomputes `wellness_scores.health_score`/`health_score_version`/`total_deductions`/`deductions_by_attribute` for all of them (not just post-V2 rows) under the new equal-weight rule, so trends/history are consistent retroactively. Explicit go-ahead for this was given in conversation.

## P4 — Known bugs / UX gaps

Real issues, not launch-blocking, but should land before or shortly after launch.

- [x] **Vet Export is fully built and has a real entry point — this item was stale.** Corrected 2026-07-18: `src/components/PetProfileContent.jsx` (the current Pet Profile, both the standalone `/pet/:petId` page and the inline card embedded in the Pets tab) has its own "Vet Report" nav card linking directly to `/pet/:petId/export` — no CareMenu involved. This item previously framed the fix as adding a `CareMenu.jsx` entry or "a second entry point on the profile header" — that second entry point already existed by the time this was re-checked. **`src/components/CareMenu.jsx` (the hamburger-menu trigger on the legacy `PetProfileTabs.jsx` page, and on `Documents.jsx`/`Insurance.jsx`) is itself slated for deprecation** — product direction is that all navigation should go through the bottom nav (Home/Pets/Menu) and back, not a per-page hamburger menu — so it should not receive further investment. `src/pages/About.jsx:38`'s "exportable reports" text still has no link, which is a real, separate, minor gap (unrelated to CareMenu) still worth a one-line fix. **New, higher-priority finding surfaced while checking this:** if CareMenu is removed as currently planned, three destinations it's the *only* path to become unreachable via any UI — see the new item below.
- [ ] **CareMenu deprecation will orphan History, Documents, and Insurance if removed without a replacement.** Confirmed 2026-07-18: of everything `CareMenu.jsx` currently links to, Vet Report/Baseline/Medications/Food/Vaccinations/Health Records all already have independent entry points via the current Pet Profile's nav cards, and AI/Pet Sitter both have independent `Menu → AI` / `Menu → Pet Sitter` pet-picker directories (`AIMenu.jsx`, `PetSitterMenu.jsx`) that don't touch CareMenu at all. But **History** (`LogHistory.jsx`, via `PetProfileTabs.jsx?tab=history`), **Documents** (`src/pages/Documents.jsx`), and **Insurance** (`src/pages/Insurance.jsx`) have no other link anywhere in the app — all three are only reachable via CareMenu or a hand-typed URL today. Before removing CareMenu, decide for each of these three: give it a real entry point elsewhere (e.g. a Pet Profile nav card, matching the pattern used for Vet Report), or confirm it's fine to become URL-only / effectively retired.
- [ ] Shared/co-owned pets show no Wellbeing chips — `SharedPetRow` function in [src/pages/Pets.jsx:196](src/pages/Pets.jsx:196) (rendered at line 158) is still a bare identity link with no chip UI.
- [ ] Pets-tab lost its "tap the ring to start Daily Check-In" shortcut when rings were replaced with non-interactive chips (UX regression, not a spec violation). Location: the `context === 'pets'` Wellbeing-chip block in [src/components/PetProfileContent.jsx:607](src/components/PetProfileContent.jsx:607) onward.
- [ ] `WeightQuickLogSheet`'s date is still UTC, inconsistent with the rest of `PetProfileContent.jsx` (which is timezone-aware per the User Profile & Timezone Settings V1 work).
- [ ] Pet Profile's standalone Wellness rings still show legacy "Stable/Improving/Monitor/Lower" wording — confirmed still live at [src/components/PetProfileContent.jsx:37](src/components/PetProfileContent.jsx:37) (`STATUS_TONE` map) and line 522 (trend-label map); explicitly out of scope when Health Score V2 shipped, per the comment at lines 595-606 of the same file.
- [ ] Home screen: no tap-target audit confirmation on full-card assumption, and no request-race guard in `Home.jsx`'s `loadData()` — both explicitly marked non-blocking when raised.
- [ ] Demo pet photos live outside the normal per-user storage path as a manual workaround so `reset-sandbox-account`'s cleanup sweep won't delete them — fragile if that sweep logic ever changes.
- [ ] Legacy pet photos (Harper/Auggie/Tribble) may still point at `base44.app` hosting — unconfirmed whether those URLs still resolve, flagged after the account-recovery incident.
- [ ] **Multi-select Health/Wellbeing check-in flow needs manual QA before shipping.** The equal-weight, multi-select redesign (`DailyCheckInSheet.jsx`, `checkinClient.js`, `scoring.js`, migration `0022`) is covered by the unit test suite (lint + 61 tests pass) but was not driven live in the browser — the local dev server requires a real signed-in account and no credentials were available/used in this session. Before shipping: manually log a check-in with 2+ symptoms in one category (e.g. Bathroom: Straining + Blood noticed), confirm the Health Score deducts correctly, confirm the Home/Pet Profile direction arrows and the Observations chip ("Changed" for 2+ symptoms) render as expected, and spot-check that the migration 0022 backfill produced sane historical scores against a few real pets.
- [ ] **Migration `0022_health_score_equal_weight_multiselect.sql` hardcodes the multi-select category list and the vomiting→'none' baseline mapping independently in raw SQL**, duplicating `MULTI_SELECT_CODES`/`BASELINE_VALUES` in `src/lib/checkin/checkinClient.js`. Since applied migrations aren't rewritten, this can't be de-duplicated retroactively — flagging so the next backfill-style migration doesn't copy-paste the same duplication forward without a shared source of truth.
- [ ] **No explicit Terms of Service / Privacy Policy acceptance at signup.** `/terms` and `/privacy` are readable in-app (Menu), but Register (`src/pages/Register.jsx`) has no "I agree to the Terms of Service and Privacy Policy" checkbox linking to them. Not a store requirement (neither Apple nor Google mandates a checkbox for non-subscription apps), so fine to land after launch — but worth adding for the liability/indemnification language in Terms §19-20 to be provably agreed-to.

## P5 — Technical debt

Not urgent; fine to carry into post-launch iteration.

- [ ] `markSkipped` ([src/lib/checkin/checkinClient.js:354](src/lib/checkin/checkinClient.js:354)) / `saveChangedCheckIn` ([src/lib/checkin/checkinClient.js:378](src/lib/checkin/checkinClient.js:378)) aren't truly transactional — sequential network calls, no shared transaction. Reordered to fail safe (delete-before-upsert), not bulletproof. A real fix needs a Postgres RPC wrapping both writes.
- [ ] Direction-read defense-in-depth (`.order('created_at')` + first-match, see comment at [src/lib/checkin/checkinClient.js:191](src/lib/checkin/checkinClient.js:191) and :247) is a safety net, not a guarantee — it only holds because `saveChangedCheckIn` clears prior observations first (comment at line 421). If that invariant is ever removed, the ordering-based dedup stops guaranteeing correctness.
- [ ] Demo/test account-type checks compare against lowercase string literals in a few places instead of shared constants — drift risk if canonical labels ever change casing.
- [ ] README's account-deletion checklist note is stale — code already does real `auth.admin.deleteUser`, doc implies otherwise.

## Not scoped yet (mentioned, no design exists)

- [ ] Weekly/monthly "no-guilt" check-in cadence (1–3 week and month+ modes beyond daily) — described as a philosophy goal, never designed.
- [ ] Auto-populate agent to keep demo data fresh on a schedule — deferred pending the check-in data model, which is now built; still unstarted.
- [ ] Fi/Tractive pet tracker integration.
- [ ] Native push reminders (blocked on Capacitor; calendar `.ics` export works as a stopgap today).
